Assume the role of a Claude Code expert.
I am preparing for the Claude Architect exam.

Provide a minimalistic POC (Python, using the Anthropic SDK) that demonstrates:

- Implementing agentic loop control flow that continues when stop_reason is
"tool_use" and terminates when stop_reason is "end_turn"
                                                                                                                                     
Add concise inline comments that explain the WHY behind each key line —
not what the code does, but why that specific approach is correct for the 
agentic loop pattern.

Keep all this information under chapter-01 folder (if not available create one)
Provide README.md file contain step by step guide to run code.
And env file to configure nay variable if needed